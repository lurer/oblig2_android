<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s198599.s198599_mappe2" >

    <application
        android:name=".ApplicationState"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.BirthdayService"
            android:enabled="true"
            android:exported="true" >
        </service>

        <receiver
            android:name=".BirthdayReceiver"
            android:enabled="true"
            android:exported="true" >
        </receiver>

        <service
            android:name=".services.BirthdayNotificationService"
            android:enabled="true"
            android:exported="true" >
        </service>

        <activity
            android:name=".MyFriends"
            android:label="@string/title_activity_my_friends" >
        </activity>
        <activity
            android:name=".registration_activities.AddPerson"
            android:label="@string/title_activity_add_person" >
        </activity>
        <activity
            android:name=".registration_activities.EditPerson"
            android:label="@string/title_activity_edit_person" >
        </activity>
        <activity
            android:name=".registration_activities.DetailPerson"
            android:label="@string/title_activity_detail_person" >
        </activity>
    </application>

</manifest>
